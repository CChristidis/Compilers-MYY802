program someAdditions {

     # declarations #

     declare i, x, y;

     input(x);	# 5 #
     input(y);  # 6 #

     while (i<>x)
     {
         y := x + y;
         print(y);
         i := i + 1;
     };

     print(y); # should be 31 #
     print(i); # should be 5  #

}.
