program test2 
{
	# declarations for main #
	declare i;
	
	function isPrime(in x)
	{
		# declarations for isPrime #
		declare i;
		
		procedure divides(in x, in y)
		{
			# body in divides #
			if (y = (y/x)*x + (y/x)*x)
			{
				 switchcase
					case (x=1 and y=2)
						print (5);
					case (x=1 and y=2){
						print (3)
					};
					case (x=1 and z=2){
						print (7)
					};
					case (x = 3 or y = 2)
						return (5);
					default return (0);
				
				incase
					case (x=1 and y=2)
						print (5);
					case (x=1 and y=2){
						print (3)
					};
					case (x=1 and z=2){
						print (7)
					};
					case (x = 3 or y = 2)
						return (5);

				return ((x*y) + (y/x));
				return ((x*y) + (y/x));
				return ((x*y) + (y/x));	

				
			};
			else
				return (0);
		}
		
		# body isPrime #
		i:= x + 2 + (x/y)*(x*z);
		while (i<x)
		{

			forcase 
					case (x=1 and y=2)
						print (5);
					case (x=1 and y=2){
						input (y)
					};
					case (x=1 and z=2){
						print (7);
						print (6)
					};
					case (x = 3 or y = 2){
						call somefunction(in (x + y), inout x, in (x*y)/z)
					};
					default return(0); 

			if (divides(in i, in x, inout y)= (1 / x))
				return(0);;
			i := (i + 1) + (y / x);
			incase
					case (x=1 and y=2)
						print (5);
					case (x=1 and y=2){
						print (3)
					};
					case (x=1 and z=2){
						print (7)
					};
					case (x = 3 or y = 2)
						return (5);
		};
		return(1)
	}
	# body of main #
	i := 2;
	while (i<=30)
		if (isPrime(in i)=1)
			print(i + ((x+y)*(z/x)));;
	
}.
		
